<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zmap.his.mapper.PatientRegistrationMapper">
	<select id="selectTop1" resultType="PatientRegistration">
		select top 1 * from PatientRegistration order by createTime asc
	</select>
</mapper>